/** 제목 분석의 알고리즘 설정입니다. 비밀값은 이 파일에 두지 않습니다. */
export const CONFIG = Object.freeze({
  MODEL: "solar-pro3",
  MIN_SEMANTIC_MATCH_SCORE: 0.65,
  MIN_CLICKBAIT_SCORE: 0.65,
  RENDER_TIMEOUT_MS: 15_000
});
