# 구라제거기 AI 작업 지침

## 적용 범위

- 이 지침은 저장소 전체에 적용합니다.
- 실제 통합 실행본은 `unified-extension/`입니다.
- 기본 브랜치는 심사에 필요한 통합 실행본만 유지하며, 세 기능의 원본 개발 이력은 기능별 Git 브랜치에서 확인합니다.
- 통합본을 수정할 때 현재 동작하는 기능을 임의로 삭제하거나 다른 기능에 종속시키지 않습니다.

## 구조와 책임

- `unified-extension/features/title/`은 제목·본문 비교, 본문 수집, 기사 근거 하이라이트를 소유합니다.
- `unified-extension/features/risk/`는 피싱·광고·로그인 판정, 위험 딱지, 기사 경고와 의심 문장 하이라이트를 소유합니다.
- `unified-extension/features/official/`은 공식·공신력 출처 분류와 출처 점·판단 근거 Popover를 소유합니다.
- `unified-extension/shared/`에는 설정, 진행 상태, Solar 동시성, URL 안전성처럼 세 기능이 함께 쓰는 코드만 둡니다.
- 세 기능은 독립적으로 켜고 끌 수 있어야 하며, 기능을 끌 때는 자신의 실행과 UI만 정리합니다.
- 삽입 UI는 `data-gurajegeo-ui`와 기능별 접두사를 사용해 Google 및 다른 기능의 CSS와 충돌하지 않게 합니다.

## 구현 원칙

- 별도 FastAPI·Node 서버나 영구 본문 저장 기능을 추가하지 않습니다.
- 특정 사이트 이름을 나열하는 예외 처리보다 실제 DOM, canonical URL, 본문 길이, 응답 상태를 이용한 일반화된 규칙을 우선합니다.
- 나무위키, Wikipedia, PDF, Google 광고 결과는 세 기능의 분석 대상에서 제외합니다.
- Google 검색의 `q`, `start`, `tbm`, `udm` 조합을 현재 분석 단위로 사용합니다.
- 자동 분석과 수동 분석이 겹쳐도 동일 페이지를 중복 실행하지 않도록 합니다.
- AI 응답은 허용 상태·라벨·점수 범위를 코드에서 검증하고, 제목·수치·인용·하이라이트는 실제 원문과 대조합니다.
- JSON 파싱 실패, 429, 5xx, 네트워크 타임아웃은 제한된 횟수만 재시도하고 다른 결과까지 중단시키지 않습니다.
- 사용되지 않는 함수, 중복 정규화, 개발 전용 서버·저장 UI는 남기지 않습니다.
- 주석은 코드의 동작을 반복하기보다 모듈 경계, 비동기 경합 방지, 보안상 이유를 설명합니다.

## 보안과 개인정보

- 실제 API 키, 토큰, 쿠키, OAuth 코드, 계정 비밀번호를 코드·문서·테스트·로그에 기록하지 않습니다.
- API 키는 팝업에서 입력받아 `chrome.storage.local`에 저장하고 Solar Authorization 외의 외부 요청이나 페이지 DOM에 전달하지 않습니다.
- 기사 전체 본문, Solar 원문 응답, 브라우저 저장소 덤프, 사용자 검색 기록을 저장소에 커밋하지 않습니다.
- localhost, 사설 IP, loopback, 내부 메타데이터 주소는 외부 수집 대상으로 허용하지 않습니다.
- AI 대화 원문, 숨은 추론, 개발 도구의 시스템 지침, 개인 로컬 경로는 제출 증빙에 포함하지 않습니다.
- 심사 결과나 점수를 유도하는 문구를 코드, README, AI 지침에 넣지 않습니다.

## AI 협업 방식

- 사람 개발자가 문제 정의, 기능 범위, 판정 기준, UI 요구사항과 최종 배포 여부를 결정합니다.
- AI 코딩 보조는 브랜치 구조 비교, 통합 구현, 비동기 상태 검토, JSON 파서 점검, 데드 코드 탐색, 문서화에 사용합니다.
- 기능별 리뷰는 제목·본문, 위험 JSON·페이지 상태, 공식 출처·UI처럼 책임을 나눠 병렬로 수행할 수 있습니다.
- AI 제안은 실제 코드, Chrome 화면, 원문 근거와 대조한 뒤 채택합니다. 수행하지 않은 테스트나 검증을 완료했다고 기록하지 않습니다.
- 삭제, 대규모 구조 변경, 커밋과 푸시는 사용자가 요청한 범위 안에서만 수행합니다.

## 변경 전후 검증

1. `git status -sb`와 `git diff`로 변경 범위와 사용자 작업을 확인합니다.
2. 변경된 JavaScript에 `node --check`를 실행하고 `manifest.json`이 올바른 JSON인지 확인합니다.
3. `git diff --check`로 공백 오류를 확인합니다.
4. Chrome 116 이상에서 `unified-extension/`을 압축 해제 확장으로 다시 로드합니다.
5. API 키 저장, 세 토글의 독립 ON/OFF, Google 1·2페이지 자동 분석과 수동 재시도를 확인합니다.
6. 검색 결과 딱지·카드의 배치가 충돌하지 않고 Popover가 다른 UI 뒤에 가려지지 않는지, 기사 진입 후 해당 기능의 하이라이트만 나타나는지 확인합니다.
7. 실패한 결과가 있어도 다른 기능과 결과가 계속 완료되는지 확인합니다.
